md-dialog(flex='80', aria-label='{{ title }}')
  form(name='frm')
    md-toolbar.md-accent
      .md-toolbar-tools
        h2 {{ title }}
    md-dialog-content(layout-padding='20')
      div(layout-gt-sm='row')
        md-input-container.md-block(flex-gt-sm)
          label Title
          input(type='text', ng-model='media.title')
        md-input-container.md-block(flex-gt-sm)
          label Album
          input(type='text', ng-model='media.album')
      div(layout-gt-sm='row')
        md-input-container.md-block(flex-gt-sm)
          label Artist
          input(type='text', ng-model='media.artist')
        md-input-container.md-block(flex-gt-sm)
          label Source URL
          input(type='text', ng-model='media.sourceUrl')
      div(layout-gt-sm='row')
        md-input-container.md-block(flex-gt-sm)
          label Media URL:
          input(type='text', name='mediaUrl', ng-model='media.mediaUrl', placeholder='mp3/ogg path', required, ng-pattern='/\.(mp3|ogg)$/', md-minlength='9')
          div(ng-messages='frm.mediaUrl.$error', role='alert', multiple)
            .my-message(ng-message='required') You must supply a url.
            .my-message(ng-message='pattern')
              | That doesn't look like a valid url
            .my-message(ng-message='md-minlength')
              | Only .MP3 &amp; .OGG files supported
    md-dialog-actions()
      md-button.md-raised.md-primary(ng-click='save(media)') Save
      md-button.md-raised.md-warn(ng-click='close()') Close
